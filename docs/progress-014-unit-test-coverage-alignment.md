# 项目进展记录 014 - 单元测试覆盖补齐

## 1. 里程碑说明

本次围绕现有功能按阿里巴巴 Java 开发手册中的单元测试规范，对已有测试进行了补充和收口，完成了核心规则单元测试、部门管理集成测试补强和全量测试验证。

完成时间：2026-04-17

## 2. 本次完成内容

### 2.1 在旧测试文件上补充新功能测试

本次优先在已有测试文件上补充了新功能相关断言：

- `UserApplicationServiceTest`
  - 补充创建用户时部门 `ldap_dn` 回写断言
  - 补充更新用户时部门 `ldap_dn` 回写断言
  - 补充停用部门禁止用户绑定的异常分支

- `DepartmentApplicationServiceTest`
  - 补充部门更新后子孙 `ancestor_path`、`dept_level` 级联更新断言
  - 补充部门删除成功路径

- `PrototypeIntegrationTest`
  - 补充部门创建、详情、更新、树查询、删除的完整集成链路

### 2.2 新增核心规则单元测试

为保证核心业务规则具备显式回归测试，本次新增：

- `DefaultPermissionLevelRuleServiceTest`
  - 生效权限等级计算
  - 用户基础信息修改边界
  - 敏感操作边界
  - 菜单维护边界
  - 部门维护边界
  - 角色菜单绑定权限等级约束

- `CasbinPolicyServiceTest`
  - Casbin 策略刷新
  - 应用启动初始化策略装载

### 2.3 测试设计对齐

本次测试补充遵循以下原则：

- 单一职责测试
- 正常分支与异常分支分开
- 核心规则类必须显式断言业务结果
- 新功能优先补到原有测试文件
- 通过集成测试验证跨层协同，而非仅靠 Mock 覆盖

## 3. 测试覆盖范围

当前已覆盖的主要功能包括：

- 认证登录与当前用户信息
- JWT 签发与过滤器解析
- 用户创建、更新、状态变更、删除、改密、重置密码
- 角色创建、更新、删除、菜单绑定、用户分配角色
- 菜单创建、更新、删除、树查询
- 部门创建、更新、删除、树查询、父子层级校验
- `permission_level` 规则判断
- Casbin 策略刷新
- LDAP stub 行为
- SQL 日志格式化
- 全局异常处理
- TraceId 透传

## 4. 文档更新

已同步更新：

- `docs/project-design.md`

补充内容包括：

- 单元测试规范章节
- 当前已落地测试文件清单
- 当前全量测试结果
- 测试章节编号与结构整理

## 5. 验证结果

本次改动后已执行全量测试，并全部通过。

执行命令：

```powershell
.tools\apache-maven-3.9.6\bin\mvn.cmd test
```

测试结果：

- Tests run: `70`
- Failures: `0`
- Errors: `0`

## 6. 下一步建议

下一轮建议继续补充以下测试能力：

1. 飞书导入场景的幂等更新测试
2. 部门与用户批量同步的回归测试
3. Spring LDAP 真实实现的适配测试
4. Jacoco 覆盖率统计与质量门禁接入
